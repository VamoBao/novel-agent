# 环境变量配置示例：复制为 .env 并填入真实值（.env 已被 gitignore 排除）

# DeepSeek API Key（必填）
DEEPSEEK_API_KEY=

# 模型名（可选，默认 deepseek-flash；如 deepseek-v4-flash / deepseek-v4-pro）
DEEPSEEK_MODEL_NAME=

# SQLite 数据库路径（可选，默认 data/novel.db）
NOVEL_DB_PATH=
