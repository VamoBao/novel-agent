# {{PROJECT_NAME}}

> **模板适配说明**（落盘前删除本块）：本模板为入口文档示例。落盘前替换所有 `{{...}}` 占位符为项目实际情况（技术栈、目录结构、模块划分按勘察与适配结论填写）；若项目不启用测试或不使用 git，删除对应条目。

{{PROJECT_DESC：一句话说明项目是什么、解决什么问题}}

## 技术栈

{{TECH_STACK：语言、框架、包管理器及关键版本}}

## 目录结构与模块划分

{{DIRECTORY_STRUCTURE：核心目录说明，标注每个模块的职责与边界}}

> **维护约定**：当项目发生重大修改（新增目录或模块、调整模块职责与边界、技术栈变动）时，Agent 须同步更新本节，保持入口文档与项目实际结构一致。

## 工作流（Agent 必读）

每次执行用户需求前，按以下顺序工作：

1. 先读本文件了解项目结构与约定，再读取目标模块目录内的 `ARCHITECTURE.md` 与 `PROGRESS.md`（不存在则视为新建模块，在任务过程中按需创建）
2. 按任务类型读取对应指引：
   - 新需求开发：`docs/feature-check-guide.md`
   - 修复 Bug / 处理报错：`docs/bug-fix-guide.md`
   - {{COMMIT_GUIDE_LINE：执行提交前读取 `docs/git-commit-guide.md`。用户选择手动提交时改为「用户显式下达提交指令时读取」；非 git 项目删除本条}}
3. 完成后按指引要求同步更新状态文档（见下方约定）

## 状态文档约定

每个模块目录内维护三份状态文档（无明确模块划分的项目放项目根目录），由 Agent 在任务过程中按需创建与更新：

- `ARCHITECTURE.md`：模块架构蓝图、文件职责与依赖边界
- `PROGRESS.md`：进度与已知 Bug（时间格式统一为 `YYYY-MM-DD`）
- `DECISIONS.md`：重大决策记录（决策背景、备选方案、权衡依据与结论）
